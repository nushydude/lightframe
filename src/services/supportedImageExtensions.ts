import supportedImageExtensions from '../../supported-image-extensions.json';

/** Canonical extension list shared by direct-open UI and validated against the Rust scanner. */
export const SUPPORTED_IMAGE_EXTENSIONS: readonly string[] = supportedImageExtensions;
